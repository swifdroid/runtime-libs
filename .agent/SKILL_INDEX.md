# Skill Index

Operational-skill router for `core-libs`.

No repository-specific skill file is required by default. Follow architecture owners and `WORKFLOW.md` directly.

Create a dedicated `.agent/skills/*.md` procedure only when a repeated operational workflow becomes complex enough that stable step-by-step guidance is valuable.

